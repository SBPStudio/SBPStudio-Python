# SBP Studio 0.5.0 — development & packaging toolchain
#
#     pip install -r requirements.txt
#     pip install -r requirements-dev.txt

-r requirements.txt

# ── Testing ─────────────────────────────────────────────────────────────────
pytest>=7.0
pytest-timeout

# ── Packaging ─────────────────────────────────────────────────────────────────
pyinstaller>=6.6

# ── Optional CPU FFT accelerator (pyfftw) ───────────────────────────────────
# FFTW-backed FFTs, auto-activated at import time when present. Uncomment to
# bundle into the build (works in both CPU and CUDA variants).
# pyfftw>=0.13
